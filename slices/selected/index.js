import { createSlice } from '@reduxjs/toolkit'

const initialState = {
    selectedTab: 0,
    selectedSection: 0,
    selectedDecoded: 0,
    selectedSimplfied: 0,
}

const selected = createSlice({
    name: selected,
    initialState,
    reducers: {

    }
});

export const {

} = selected.actions
export default selected.reducer